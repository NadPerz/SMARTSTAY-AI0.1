# Processed knowledge outputs

Use this folder for chunked/normalized knowledge assets produced from `knowledge_base/documents`. The knowledge-base teammate should implement the processing pipeline outputs consumed by retrieval.
