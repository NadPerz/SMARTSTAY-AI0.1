# Agent ownership

Use this ownership map when assigning implementation work: Concierge teammate owns intake/orchestration flows, Reservation teammate owns booking lifecycle and policy checks, Recommendation teammate owns ranking and personalization, and Feedback teammate owns review ingestion and sentiment loops. Shared protocol/test contracts remain under `agents/common` and are coordinated across all agent owners.
