# Embedding assets

Place generated embedding files and embedding metadata in this folder. The retrieval teammate should add embedding generation/update workflows aligned with the project retrieval blueprint.
