# Vector index artifacts

Store persisted vector index files and index metadata used by retrieval here. The retrieval teammate should implement the chosen vector store layout and lifecycle scripts next.
